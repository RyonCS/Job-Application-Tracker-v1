<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/editandAddJob.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
    <title>Add a new job!</title>
</head>
<body>
          <!-- Header -->
  <header class="p-3 text-bg-dark">
    <div class="container-fluid">
        <div class="d-flex align-items-center justify-content-between">

            <!-- Back to My Jobs -->
            <a href="/jobs/myJobs" class="btn btn-outline-light me-3">Back to My Jobs</a>

            <!-- Title centered -->
            <h3 class="text-white m-0 mx-auto">Job Application Tracker</h3>

            <!-- Logout Button -->
            <form action="/auth/logout" method="post" class="ms-3">
                <button class="btn btn-outline-light">Log Out</button>
            </form>
        </div>
    </div>
</header>

    <form action="/jobs/myJobs" method="POST">
        <table id="editJobsTable">
        <!-- Semantically incorrect for not having headers in <thead> but quickest workaround to make button take up two rows.-->
        <tbody>
            <tr>
                <th>
                    Date
                </th>
                <th>
                    Company
                </th>
                <th>
                    Position
                </th>
                <th>
                    Location
                </th>
                <th>
                    Status
                </th>
                <th>
                    Link to Job
                </th>
                <td rowspan="2">
                    <button>Submit</button>
                </td>
            </tr>
            <tr>
                <td>
                    <% const formattedDate = date ? date.toISOString().split('T')[0] : '' %>
                    <input type="date" name="date" placeholder="<%=formattedDate %>" value="<%=formattedDate %>" style="padding: 3px;">
                </td>
                <td>
                     <input type="text" name="company" placeholder="Company">
                </td>
                <td>
                    <input type="text" name="position" placeholder="Position">
                </td>
                <td>
                     <input type="text" name="location" placeholder="Location">
                </td>
                <td>
                     <select name="status">
                        <option value="Applied">Applied</option>
                        <option value="Interview">Interview</option>
                        <option value="Offer">Offer</option>
                        <option value="Rejected">Rejected</option>
                    </select>
                </td>
                <td>
                    <input type="text" name="linkToPosting" placeholder="Link">
                </td>
            </tr>
        </tbody>
    </table>

    </form>

    <!-- 100% privacy-first analytics -->
    <script async src="https://scripts.simpleanalyticscdn.com/latest.js"></script>

</body>
</html>